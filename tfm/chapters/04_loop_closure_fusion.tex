\chapter{Loop Closure Semantic Fusion}
\label{ch:loop_closure_fusion}

% Analysis of the impact of each criteria on rejection-acceptance
% -> Compute quantitative metrics with respect to rejection/acceptance of each criteria (todo)
% -> Which criteria has more impact on false rejections
% -> Does the semantic descriptor have an impact

% Proposed geometric modifications:
% - bidirectional geometric comparison (done)
% - coocurence visibility (done)
%   -  propose an heuristic to only compute one direction (not done)
% - bounding-boxes:
%   - distances between them (done) -> Conclusion it does not have an impact
%   - intersection between them (to do)

% Analysis of using more spatial specific descriptor: SigLip, PE-Core (best), PE-Spatial
% -> bbox w/o background
% -> bbox w background (not done)
