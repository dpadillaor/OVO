"""SLAM backbones.

`slam_module` config tokens select a backbone and double as the config directory
name (data/working/configs/slam/<token>/). Supported tokens: `simulated`,
`orbslam2`/`orbslam3`, `vanilla`, `gaussian_slam`.
"""
