default_dataset: Replica

experiments:
  - label: test-instance-ap
    scenes_id: office0
    stages: [segment, eval_instances]
    ovo_config:
      slam:
        slam_module: groundtruth
        close_loops: true
      semantic:
        fusion_method: clip
