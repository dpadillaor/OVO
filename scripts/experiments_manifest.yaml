# OVO Experiments Manifest
# This file defines the list of experiments to be run by the Orchestrator.

# Default dataset for the experiments (can be overridden per experiment if necessary)
default_dataset: "Replica"

# List of Experiments
experiments:
  # ===========================================================================
  # CLIP Fusion
  # ===========================================================================
  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "CLIP"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.001
        rotation_noise_std: 0.01

  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "CLIP"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.005
        rotation_noise_std: 0.01

  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "CLIP"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.01
        rotation_noise_std: 0.01

  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "CLIP"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.05
        rotation_noise_std: 0.01

  # ===========================================================================
  # PE-Core Fusion
  # ===========================================================================
  - label: "Core-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Core-L14-336"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.001
        rotation_noise_std: 0.01

  - label: "Core-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Core-L14-336"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.005
        rotation_noise_std: 0.01

  - label: "Core-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Core-L14-336"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.01
        rotation_noise_std: 0.01

  - label: "Core-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Core-L14-336"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.05
        rotation_noise_std: 0.01

  # ===========================================================================
  # PE-Spatial Fusion
  # ===========================================================================
  - label: "Spatial-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Spatial-L14-448"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.001
        rotation_noise_std: 0.01

  - label: "Spatial-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Spatial-L14-448"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.005
        rotation_noise_std: 0.01

  - label: "Spatial-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Spatial-L14-448"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.01
        rotation_noise_std: 0.01

  - label: "Spatial-Fusion_Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "PE"
        pe:
          model_card: "PE-Spatial-L14-448"
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.05
        rotation_noise_std: 0.01

  # ===========================================================================
  # SAM3 Fusion
  # ===========================================================================
  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "SAM3"
        sam3:
          components: "vit_only"
          load_from_hf: True
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.001
        rotation_noise_std: 0.01

  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "SAM3"
        sam3:
          components: "vit_only"
          load_from_hf: True
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.005
        rotation_noise_std: 0.01

  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "SAM3"
        sam3:
          components: "vit_only"
          load_from_hf: True
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.01
        rotation_noise_std: 0.01

  - label: "Rerun"
    scenes_id: "office0"
    stages: ["run", "segment", "eval"]
    ovo_config:
      semantic:
        fusion_method: "SAM3"
        sam3:
          components: "vit_only"
          load_from_hf: True
      slam:
        slam_module: "groundtruth"
        close_loops: True
    slam_config:
      noise:
        translation_noise_std: 0.05
        rotation_noise_std: 0.01
